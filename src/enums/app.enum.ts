export enum EnvType {
    dev = "dev",
    testing = "testing",
    staging = "staging",
    prod = "prod",
}

export enum AuthProvider {
    GOOGLE = "google.com",
    APPLE = "apple.com",
    EMAIL = "email",
  }